export class Sorting {
    constructor(
        public array: number[],
        public compareElements: number[],
        public running: boolean,
        public sortingDelay: number,
        public algorithm: string
    ) {

    }
}